name: Release rapidsim

on:
  repository_dispatch:
    types: [rapid-release]

  workflow_dispatch:
    inputs:
      rapid_tag:
        description: "RAPID release tag to test"
        required: true
        default: "v2.2.3"

jobs:
  prepare-release:
    name: Prepare release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout rapidsim
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Determine RAPID release tag
        id: release
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            rapid_tag="${{ inputs.rapid_tag }}"
            echo "TEST MODE"
          else
            rapid_tag="${{ github.event.client_payload.rapid_tag }}"
            echo "RELEASE MODE"
          fi

          echo "RAPID tag: $rapid_tag"
          echo "rapid_tag=$rapid_tag" >> "$GITHUB_OUTPUT"

      - name: Calculate version
        id: version
        shell: bash
        run: |
          rapid_tag="${{ steps.release.outputs.rapid_tag }}"
          rapid_version="${rapid_tag#v}"

          current_rapidsim=$(python - <<'PY'
          import re

          with open("pyproject.toml") as f:
              text = f.read()

          match = re.search(
              r'(?m)^version\s*=\s*"(\d+\.\d+\.\d+)"',
              text
          )

          if not match:
              raise SystemExit("Could not determine rapidsim version")

          print(match.group(1))
          PY
          )

          previous_rapid_version=$(
            git -C external/RAPID describe --tags --abbrev=0 \
              | sed 's/^v//' \
              | sed 's/-.*//'
          )

          echo "Previous RAPID:  $previous_rapid_version"
          echo "New RAPID:       $rapid_version"
          echo "rapidsim current: $current_rapidsim"

          IFS='.' read -r current_major current_minor current_patch <<< "$current_rapidsim"
          IFS='.' read -r prev_major prev_minor prev_patch <<< "$previous_rapid_version"
          IFS='.' read -r new_major new_minor new_patch <<< "$rapid_version"

          if [ "$new_major" -gt "$prev_major" ]; then
            next_major=$((current_major + 1))
            next_minor=0
            next_patch=0
          elif [ "$new_minor" -gt "$prev_minor" ]; then
            next_major=$current_major
            next_minor=$((current_minor + 1))
            next_patch=0
          else
            next_major=$current_major
            next_minor=$current_minor
            next_patch=$((current_patch + 1))
          fi

          next_version="${next_major}.${next_minor}.${next_patch}"

          echo "New rapidsim:    $next_version"

          echo "version=$next_version" >> "$GITHUB_OUTPUT"

      - name: Test mode summary
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "======================================"
          echo "RAPIDSIM RELEASE TEST"
          echo "======================================"
          echo "RAPID release:   ${{ steps.release.outputs.rapid_tag }}"
          echo "rapidsim current: $(python - <<'PY'
          import re
          text = open("pyproject.toml").read()
          print(re.search(r'(?m)^version\s*=\s*"(\d+\.\d+\.\d+)"', text).group(1))
          PY
          )"
          echo "rapidsim next:   ${{ steps.version.outputs.version }}"
          echo ""
          echo "TEST ONLY:"
          echo "- no submodule update"
          echo "- no file changes"
          echo "- no commit"
          echo "- no push"
          echo "- no PyPI upload"
          echo "======================================"

      - name: Update RAPID submodule
        if: github.event_name == 'repository_dispatch'
        run: |
          git -C external/RAPID fetch --tags
          git -C external/RAPID checkout "${{ steps.release.outputs.rapid_tag }}"

      - name: Update rapidsim version
        if: github.event_name == 'repository_dispatch'
        env:
          NEW_VERSION: ${{ steps.version.outputs.version }}
        run: |
          python - <<'PY'
          import os
          import re

          new_version = os.environ["NEW_VERSION"]

          with open("pyproject.toml") as f:
              text = f.read()

          text, count = re.subn(
              r'(?m)^version\s*=\s*"\d+\.\d+\.\d+"',
              f'version = "{new_version}"',
              text,
              count=1,
          )

          if count != 1:
              raise SystemExit("Could not update version in pyproject.toml")

          with open("pyproject.toml", "w") as f:
              f.write(text)

          with open("setup.py") as f:
              text = f.read()

          text, count = re.subn(
              r'version\s*=\s*"\d+\.\d+\.\d+"',
              f'version="{new_version}"',
              text,
              count=1,
          )

          if count != 1:
              raise SystemExit("Could not update version in setup.py")

          with open("setup.py", "w") as f:
              f.write(text)
          PY

      - name: Commit release preparation
        if: github.event_name == 'repository_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add pyproject.toml setup.py external/RAPID
          git commit -m "release: rapidsim ${{ steps.version.outputs.version }}"
          git push